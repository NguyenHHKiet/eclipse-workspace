#sonatype

mysql-connect-java]

1. maven based project
	build tool : src, libs, build so, required jdk
	dependency @see pom.xml
		groupId, artifactId, version --> site

2. db connect
	url, user, pass, Driver
	loading, connecting , preparing sql, executing , result
	
3. 